ngDescribe({
    name: 'Test task-list component',
    modules: 'app',
    element: '<task-list></task-list>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
