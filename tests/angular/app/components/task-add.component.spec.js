ngDescribe({
    name: 'Test task-add component',
    modules: 'app',
    element: '<task-add></task-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
