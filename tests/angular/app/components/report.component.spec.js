ngDescribe({
    name: 'Test report component',
    modules: 'app',
    element: '<report></report>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
